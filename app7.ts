(function() {

// 内置对象, 首字母大写

const regexp: RegExp = /\w/         // 正则 RegExp

const date: Date = new Date()        // 日期

const time: number = new Date().getTime()     // getTime() 之后是数字类型

const error: Error = new Error('错误')

// dom和bom 的内置对象，见index.ts

})()